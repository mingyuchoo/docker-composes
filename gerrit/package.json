{
  "name": "gerrit",
  "version": "1.0.0",
  "description": "You can run docker containers for Gerrit",
  "main": "docker-compose.yml",
  "scripts": {
    "docker:up": "docker-compose --file docker-compose.yml up --build --detach",
    "docker:down": "docker-compose --file docker-compose.yml down --volumes --remove-orphans",
    "docker:exec": "docker exec -it gerrit /bin/sh",
    "docker:exec:root": "docker exec -u root -it gerrit /bin/sh"
  },
  "keywords": [
    "docker",
    "gerrit"
  ],
  "author": "mingyuchoo@gmail.com",
  "license": "MIT License"
}
