version: "3.9"

networks:
  default:
    external: true
    name: db_net

services:
  jenkins:
    image: "jenkins/jenkins:latest"
    container_name: jenkins
    ports:
      - 8888:8080
      - 50000:50000
    environment:
      - TZ='Asia/Seoul'
