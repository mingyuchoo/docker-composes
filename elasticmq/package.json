{
  "name": "elasticmq",
  "version": "1.0.0",
  "description": "You can run docker containers for elasticmq",
  "main": "docker-compose.yml",
  "scripts": {
    "docker:up": "docker-compose --file docker-compose.yml up --build --detach",
    "docker:down": "docker-compose --file docker-compose.yml down --volumes --remove-orphans",
    "docker:exec": "docker exec -it elasticmq-db /bin/sh",
    "docker:exec:root": "docker exec -u root -it elasticmq-db /bin/sh"
  },
  "keywords": [
    "docker",
    "elasticmq"
  ],
  "author": "mingyuchoo@gmail.com",
  "license": "MIT License"
}
