{
  "name": "mariadb",
  "version": "1.0.0",
  "description": "You can run docker containers for mariadb",
  "main": "docker-compose.yml",
  "scripts": {
    "docker:build": "docker-compose --file docker-compose.yml up --build --detach",
    "docker:exec": "docker exec -it mariadb /bin/bash",
    "docker:exec:root": "docker exec -u root -it mariadb /bin/bash"
  },
  "keywords": [
    "docker",
    "mariadb"
  ],
  "author": "mingyuchoo@gmail.com",
  "license": "MIT License"
}
