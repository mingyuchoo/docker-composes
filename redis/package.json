{
  "name": "redis",
  "version": "1.0.0",
  "description": "You can run docker containers for redis",
  "main": "docker-compose.yml",
  "scripts": {
    "docker:build": "docker-compose --file docker-compose.yml up --build --detach",
    "docker:exec": "docker exec -it redis-db /bin/bash",
    "docker:exec:root": "docker exec -u root -it redis-db /bin/bash"
  },
  "keywords": [
    "docker",
    "redis"
  ],
  "author": "mingyuchoo@gmail.com",
  "license": "MIT License"
}
