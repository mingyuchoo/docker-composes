version: "3.9"
  
networks:
  default:
    external: true
    name: db_net

volumes:
  data:

services:
  redis-db:
    image: redis:latest
    container_name: redis-db
    ports:
      - 6379:6379
    labels:
      - name=redis
      - mode=standalone
    volumes:
      - ./data:/data
