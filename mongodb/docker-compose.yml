version: '3.9'

# Create `my-net` if exists use it
networks:
  my-net:
    name: my-net
    driver: bridge

# Use `my-net`
# networks:
#   default:
#     external: true
#     name: my-net

# volumes:
#   db_data:

services:
  mongo:
    image: mongo
    deploy:
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    container_name: mongo-db
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_DATABASE_USERNAME: test
      MONGO_DATABASE_PASSWORD: test
      MONGO_DATABASE_NAME: test
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root
      MONGO_INITDB_DATABASE: admin
    volumes:
      - /var/docker/init/mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
      - /var/docker/init/mongod.service:/lib/systemd/system/mongod.service
      - /var/docker/init/mongod.conf:/etc/mongod.conf
      # - /var/docker/db_data:/data/db

  mongo-express:
    image: mongo-express
    container_name: mongo-express-web
    restart: always
    ports:
      - 8081:8081
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: root
