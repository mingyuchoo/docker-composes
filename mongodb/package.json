{
  "name": "mongo",
  "version": "1.0.0",
  "description": "You can run docker containers for mongo",
  "main": "docker-compose.yml",
  "scripts": {
    "docker:build": "docker-compose --file docker-compose.yml up --build --detach",
    "docker:exec": "docker exec --interactive=true --tty=true mongo-db /bin/bash"
  },
  "keywords": [
    "docker",
    "mongo"
  ],
  "author": "mingyuchoo@gmail.com",
  "license": "MIT License"
}
