{
  "name": "mysql",
  "version": "1.0.0",
  "description": "You can run docker containers for mysql",
  "main": "docker-compose.yml",
  "scripts": {
    "docker:build": "docker-compose --file docker-compose.yml up --build --detach",
    "docker:exec": "docker exec -it mysql-db /bin/bash",
    "docker:exec:root": "docker exec -u root -it mysql-db /bin/bash"
  },
  "keywords": [
    "docker",
    "mysql"
  ],
  "author": "mingyuchoo@gmail.com",
  "license": "MIT License"
}
