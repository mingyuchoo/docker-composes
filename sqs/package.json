{
  "name": "sqa",
  "version": "1.0.0",
  "description": "You can run docker containers for sqa",
  "main": "docker-compose.yml",
  "scripts": {
    "docker:up": "docker-compose --file docker-compose.yml up --build --detach",
    "docker:down": "docker-compose --file docker-compose.yml down --volumes --remove-orphans",
    "docker:exec": "docker exec -it sqa-db /bin/sh",
    "docker:exec:root": "docker exec -u root -it sqa-db /bin/sh"
  },
  "keywords": [
    "docker",
    "sqa"
  ],
  "author": "mingyuchoo@gmail.com",
  "license": "MIT License"
}
